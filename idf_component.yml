## IDF Component Manager Manifest File
##
## ESP-IDF v6.0 removed the built-in `usb` component and republished it as
## `espressif/usb` in the component registry. This manifest pulls it in for
## the patched RTL-SDR driver.
dependencies:
  idf:
    version: '>=5.2'
  espressif/usb: '*'
